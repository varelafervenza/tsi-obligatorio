# lib

Wrappers TypeScript que invocan los comandos Tauri (`invoke`) definidos en `src-tauri/src/*`: vault, auth, generator, events. Ningún secreto debe manejarse en el frontend fuera de estos wrappers.
